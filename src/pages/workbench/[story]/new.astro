---
import { redirect } from 'astro/navigation';

// Protect route: Redirect to signin if not authenticated
if (!Astro.locals.user?.id) {
  return redirect('/signin');
}
---

// Redirect /workbench/:story/new to create first chapter
export async function GET({ params }) {
  const { story } = params;

  // TODO: Create first chapter and redirect to it
  // For now, just redirect to dashboard
  throw redirect('/dashboard');
}
